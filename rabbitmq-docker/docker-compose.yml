services:  
  rabbitmq:
    image: rabbitmq:3.9-management
    container_name: rabbitmq-devs
    tty: true
    environment:
      RABBITMQ_USER: guest
      RABBITMQ_PASSWORD: guest
    ports:
      - "5672:5672"
      - "15672:15672"

networks:
  default:
    driver: bridge      